import React from 'react';
import PropTypes from 'prop-types';

const Stack = props => {
    return (
        <div>
            
        </div>
    );
};

Stack.propTypes = {
    
};

export default Stack;